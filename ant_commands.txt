ant clean build     // Removes all compiled files (clean) and then builds the project
ant build           // Compiles and packages the application
ant compile         // Only compiles the source code without packaging
ant                 // Runs the default target defined in build.xml